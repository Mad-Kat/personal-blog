---
interface Props {
	date: Date;
	class?: string;
}

const { date, class: className, ...other } = Astro.props;
---

<time datetime={date.toISOString()} class={className} {...other}>
	{
		date.toLocaleDateString("en-us", {
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
